#include <stdio.h>

int main(void)
{
    int height;
    int length;
    int width;

    float something;
    float something2;
    float something3;

    printf("%d, %d, %d\n", height, length, width);
    printf("%f, %f, %f\n", something, something2, something3);
}