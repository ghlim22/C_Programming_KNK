int main(void)
{
    int a[10], i, j, k, m;

    i = j;
    i = 10 * j + 1;       // error
    i = j - k;            // error
    i = (SQR(j) * SQR(j)) // error
        i = ((j) * (j) * (j))
            i = jk;
    puts("i"
         "j");
    i = SQR(j);
    i = (j);

    return 0;
}